{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "nixpacks"
  },
  "deploy": {
    "startCommand": "env DISABLE_DATABASE_ENVIRONMENT_CHECK=1 bundle exec rails db:drop db:create db:schema:load && bundle exec puma -C config/puma.rb",
    "healthcheckPath": "/health/live",
    "healthcheckTimeout": 60
  }
}